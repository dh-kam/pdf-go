# Go PDF Rendering Library - TODO Checklist

Korean localization: [TODO.ko.md](TODO.ko.md).

## Poppler Splash Exact100 Follow-up

- [x] Reconfirm the restored local `test/2nd` PDF corpus is present for full exact100 runs while keeping it untracked.
- [x] Revalidate the tracked `test/testdata/compare/pdfs` corpus with current no-CGo Splash defaults and Poppler `-cropbox`: `286/286` exact100, `0` errors, and `100.00000000%` pixel exact in `tmp/compare_pdfs_exact_splash_current_20260522_002020`.
- [x] Harden `cmd/pdfcompare` output safety so repository roots, scan roots, and their ancestors cannot be deleted as output directories.
- [x] Align `cmd/pdfcompare` metadata and execution by passing `pdftoppm -cropbox` whenever the HTML report says `poppler=-cropbox`.
- [x] Match Poppler's glyph phase cache gate with transformed font bbox height for rotated/sheared glyphs.
- [x] Match Poppler's FreeType face bounding box for CFF glyph cache sizing. `corpus-doc-B.pdf` pages 321 and 359 became exact100, `corpus-doc-A.pdf` pages 1-2 improved by `51,032` bad pixels, the full `test/2nd` corpus improved from `1012/1422` to `1014/1422` exact100, and the tracked corpus stayed `286/286` in `tmp/regression_286_cff_bbox_cache_codex`.
- [x] Reset unsupported ExtGState `/BM` values to `Normal` instead of preserving a previous blend mode.
- [x] Validate the CropBox lower-left transform correction on the full `test/2nd` corpus: `509/1422` exact100, `392` improved pages, and `0` regressions versus `2nd_exact100_splash_cid_width_link_20260521_213859`.
- [x] Add Poppler-aligned text rendering mode fill/stroke/clip handling. The `test/2nd` corpus remains `208/1422` exact100, but total bad pixels improved from `69,182,085` to `67,195,281`; remaining regressions are limited to tourism map pages 1-2.
- [x] Align Indexed image `/Decode` mapping with Poppler `GfxImageColorMap`: decode with the image bit-depth denominator first, then clamp to the palette high index.
- [x] Match Poppler's regular image source-row and matrix contract. The tracked corpus remains `286/286` exact100, and the `test/2nd` corpus improved from `663/1422` to `672/1422` exact100 with `0` regressions in `tmp/2nd_full_all_image_contract_20260524_020128`.
- [x] Match Poppler link annotation border precedence by honoring `/BS /W` before `/Border`. The tracked corpus remains `286/286` exact100, and `[AI Essential] 실습가이드 241128.pdf` bad pixels dropped by `584,981` with `0` regressions in `tmp/focus_link_bs_20260524_092731`.
- [x] Reapply CIDFontType2 Identity `/W` and `/DW` metrics after CID wrapping so advances are keyed by CID like Poppler `GfxCIDFont::getWidth`. The tracked corpus remains `286/286` exact100, and the full `test/2nd` corpus improved from `672/1422` to `957/1422` exact100 with `0` exact losses in `tmp/2nd_full_cid_metrics_default_20260524_100852`.
- [x] Align Poppler Base14 aliases for ArialMT, CourierNew, SymbolMT, and TimesNewRomanPS variants. The full `test/2nd` exact100 count stayed `984/1422`, but bad pixels dropped by `32,299` with `0` regressions in `tmp/2nd_full_after_font_alias_20260524_171955`.
- [x] Match Poppler's simple-font character-code width lookup before glyph-width fallback. `corpus-doc-C.pdf` p5/p6 improved by `23,851` pixels and later reached exact100 in the full latest run.
- [x] Apply FreeType non-zero fill-rule coverage to all pure-Go FreeType outline glyphs, not only Type1/CFF. The full `test/2nd` corpus improved from `984/1422` to `995/1422` exact100 with `0` regressions and bad pixels dropped by `167,471` in `tmp/2nd_full_latest_20260524_203035`; tracked corpus remained `286/286` in `tmp/regression_after_probes_20260524_210823`.
- [x] Match Poppler-style parent resource stack lookup for Form XObjects, Type3 CharProcs, and tiling-pattern replay. DallE extracted p2 became exact100, contributing one new full-corpus exact100 page.
- [x] Promote Poppler-style bounded small-repeat tiling fallback replay with full-affine tile transforms by default. The full `test/2nd` corpus remains `995/1422` exact100, bad pixels dropped from `1,035,828` to `1,028,404`, DallE p81 improved from `13,639` to `6,215` bad pixels, and there were `0` regressions in `tmp/2nd_full_after_tiling_default_20260524_223238`; tracked corpus remained `286/286` in `tmp/regression_after_tiling_default_20260524_222738`.
- [x] Reject the image `/SMask` vertical-flip scale-then-flip hypothesis for the current tourism residual. `PDF_DEBUG_SPLASH_DISABLE_TOPDOWN_VFLIP_SCALE=1` produced `0` pixel delta on `corpus-doc-A.pdf` p1/p2 in `tmp/probe_tourism_poppler_scale_flip_20260524_205858`.
- [x] Reject default CMYK image `dblToByte` truncation as an isolated fix. Poppler truncates in `GfxDeviceCMYKColorSpace::getRGBLine`, but enabling the probe regressed `FT_on_SKT_KT_dp_260102.pdf` p2 from `446` to `23,561` bad pixels, so it remains gated by `PDF_DEBUG_CMYK_IMAGE_DBLTOBYTE_TRUNCATE=1` until the surrounding CMYK Decode/JPEG path is aligned.
- [x] Match Poppler's fresh child-Splash pattern-alpha isolation for nested transparency groups by default. `DallE Prompt Book.pdf` page 81 improved from `535` to `8` bad pixels, the full `test/2nd` corpus stayed `1010/1422` exact100 with bad pixels reduced from `799,527` to `799,000`, and there were `0` exact losses and `0` bad-pixel regressions in `tmp/2nd_full_group_alpha_nested_codex_20260525_232018`; tracked corpus stayed `286/286` in `tmp/regression_286_group_alpha_nested_default_codex_20260525_235112`.
- [x] Match Poppler's nested non-isolated group `alpha0Ptr` result-alpha correction during transparency-group composite by default. `corpus-doc-A.pdf` page 2 improved from `404,983` to `375,765` bad pixels, the full `test/2nd` corpus stayed `1010/1422` exact100 with bad pixels reduced from `797,206` to `767,988`, and there were `0` exact losses and `0` bad-pixel regressions in `tmp/2nd_full_alpha0_composite_codex`; tracked corpus stayed `286/286` in `tmp/regression_286_alpha0_default_codex_20260601_224301`.
- [x] Promote Poppler-style cropped Form transparency groups by default after scoping the shading clip-bbox integer snap to cropped group stacks only. `corpus-doc-A.pdf` page 2 improved from `375,765` to `360,933` bad pixels, the full `test/2nd` corpus stayed `1010/1422` exact100 with bad pixels reduced from `767,988` to `753,156`, and there were `0` exact losses in `tmp/2nd_full_shading_snap_form_crop_scoped_codex`; tracked corpus stayed `286/286` in both `tmp/regression_286_shading_snap_default_scoped_codex` and `tmp/regression_286_shading_snap_crop_scoped_codex`.
- [x] Match Poppler's non-pattern `sc`/`SC`/`scn`/`SCN` operand-count handling: wrong component counts leave color and pattern state unchanged, and non-numeric components fall back to zero. The O2/FT focus set had `0` pixel delta in `tmp/focus_scn_arity_o2_ft_codex`, and the tracked corpus stayed `286/286` in `tmp/regression_286_scn_arity_codex`.
- [x] Promote Poppler-order raw-path Splash stroke rendering by default for pure stroke operators while keeping fill-and-stroke operators on a separate gate. The focus set improved from `7,758` to `6,486` bad pixels, the full `test/2nd` corpus improved from `1020/1422` to `1024/1422` exact100 with total bad pixels reduced from `33,839` to `31,642`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_poppler_order_default_codex`.
- [x] Match FreeType raw CFF FontMatrix/effective-UPM normalization while keeping the FreeType-Go duplicate-contour cleanup opt-in until its decode-stage semantics match C FreeType. The O2 CFF pages gained `26` exact100 pages, the full `test/2nd` corpus improved from `1025/1422` to `1051/1422` exact100 with `0` regressions and bad pixels reduced from `31,579` to `22,529` in `tmp/2nd_full_after_cffnorm_contour_off_codex_20260603_020359`; the tracked corpus stayed `286/286` in `tmp/regression_286_after_contour_default_off_codex_20260603_015725`.
- [x] Match Poppler `Splash::pipeRun` soft-mask alpha multiplication order for grouped composites by default. `corpus-doc-A.pdf` page 2 improved from `364` to `312` bad pixels, full `test/2nd` stayed `1051/1422` exact100 with `0` regressions and bad pixels reduced to `22,477` in `tmp/2nd_full_after_group_smask_order_codex_20260603_024527`; the tracked corpus stayed `286/286` in `tmp/regression_286_after_group_smask_order_codex_20260603_023705`.
- [x] Match Poppler's tiny text-fill y-min AA boundary tie for fill-and-stroke glyph paths by nudging exact integral y-min bounds one `math.Nextafter` step below the scanner `floor()` boundary before fill only. `[AI Essential] 교안 241128.pdf` page 19 became exact100, the remaining-16 focus set improved from `0/16` to `1/16` exact100 with bad pixels reduced from `6,020` to `6,018` in `tmp/probe_remaining16_ymin_default_codex_20260603_085427`, the full `test/2nd` corpus improved from `1406/1422` to `1407/1422` exact100 with `0` errors in `tmp/2nd_full_ymin_default_codex_20260603_090238`, and the tracked corpus stayed `286/286` in `tmp/regression_286_ymin_default_codex_20260603_085545`.
- [x] Match Poppler `GfxPath`/Splash raw-stroke behavior for open single-point subpaths in `StrokePathWithCTM` by dropping move-only subpaths before stroke rasterization while preserving explicit zero-length line segments. `corpus-doc-A.pdf` page 1 improved from `3,129` to `3,117` bad pixels, page 2 stayed at `312`, the remaining-16 focus set stayed `1/16` exact100 with bad pixels reduced to `6,006` in `tmp/probe_remaining16_raw_moveonly_codex_20260603_103500`, focused Splash/image tests pass, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_raw_moveonly_codex_20260603_102500`.
- [x] Reject broad tourism residual probes that do not match Poppler source behavior or regress focused pages: `PDF_DEBUG_SPLASH_TEXT_PATH_POPPLER_ORDER=1` produced no delta on tourism pages 1-2 in `tmp/probe_tourism_text_poppler_order_codex_20260603_091500`; text stroke-width scaling regressed tourism page 1 from `3,129` to `23,321+` bad pixels; separate text-path CTM rendering regressed tourism pages 1-2 to `538,045` and `55,657` bad pixels in `tmp/probe_tourism_text_path_separate_ctm_codex_20260603_094500`; and forcing the `djpeg-popplerline` branch worsened `FT_on_SKT_KT_dp_260102.pdf` page 1 from `1,633` to `3,186` bad pixels.
- [ ] Evaluate `PDF_DEBUG_SPLASH_POPPLER_ORDER_FILL_STROKE_PATH=1` separately before defaulting `B`/`B*`/`b`/`b*` stroke halves, because the broad gate passed focus/286 but has path-heavy performance risk.
- [x] Match the remaining `DallE Prompt Book.pdf` page 81 `8`px residual by defaulting the single-exponential radial shading edge-correction skip. The page became exact100, the pattern focus set improved from `3/5` to `4/5`, the full `test/2nd` corpus improved from `1014/1422` to `1015/1422` exact100, and the tracked corpus stayed `286/286` in `tmp/regression_286_radial_default_codex`.
- [x] Reject the DallE p81 Form-group parent soft-mask restore hypothesis. `GO_PDF_ENABLE_EXTGSTATE_SMASK=1 PDF_DEBUG_FORM_GROUP_RESTORE_SOFTMASK=1` produced `0` pixel delta on p81 in `tmp/probe_dalle_form_smask_restore_20260524_235529`.
- [x] Reject the DallE p81 full-affine tiling range plus ExtGState SMask hypothesis. `GO_PDF_ENABLE_EXTGSTATE_SMASK=1 PDF_SPLASH_TILING_FULL_AFFINE_RANGE=1` kept p19 at `1,356`, p2 exact100, and p81 at `6,215` bad pixels in `tmp/probe_dalle_affine_range_smask_20260525_011807`.
- [x] Reject the day3 image `/SMask` mask-downscale flip hypothesis. Disabling only the SMask bitmap `downscaleVFlipTopDown` path kept `corpus-doc-C.pdf` at `30/36` exact100 with `0` pixel delta in `tmp/probe_day3_smask_maskdown_off_20260524_174940`.
- [x] Match PNG predictor pixel stride for RGB/sub-byte rows. DallE p42/p54/p59/p71 became exact100, and the `test/2nd` corpus improved from `659/1422` to `663/1422` exact100 with `0` regressions in `tmp/2nd_full_predictor_stride_20260524_011253`.
- [x] Clear stale Pattern color state when solid color operators are applied. The 5-PDF `test/2nd` subset improved from `30,267,244` to `17,481,626` bad pixels with `0` regressions, while exact100 stayed `138/618`.
- [x] Keep Type3 CharProc glyph origins raw instead of pre-flooring them in the evaluator CTM. DallE improved from `1/82` to `46/82` exact100 with `0` regressions, and the 5-PDF subset improved from `138/618` to `183/618` exact100.
- [ ] Keep ExtGState `/SMask` Form-mask rendering behind `GO_PDF_ENABLE_EXTGSTATE_SMASK=1` until Poppler-compatible cropped group geometry, backdrop, transfer, and performance parity are validated. The current gated implementation keeps the full `test/2nd` corpus at `1000/1422` exact100 with `0` exact gains/losses, `0` regressions, and improves only `corpus-doc-A.pdf` page 2 from `486,806` to `404,988` bad pixels in `tmp/2nd_full_smask_device_bbox_codex_20260525_115506`.
- [x] Reject the first ExtGState `/SMask /BC` backdrop prototype for the current tourism residual: `GO_PDF_ENABLE_EXTGSTATE_SMASK_BC=1` produced `0` additional pixel delta versus the existing SMask gate in `tmp/probe_tourism_extgstate_smask_bc_20260524_181341`.
- [x] Reject the text hScale glyph-matrix promotion hypothesis for the current `Tr 2` residuals: `PDF_DEBUG_SPLASH_GLYPH_HSCALE_MATRIX=1` produced `0` delta on the three receipt focus pages and the four extracted `[AI Essential] 교안 241128.pdf` pages in `tmp/probe_text_hscale_receipts_on_20260524_181638` and `tmp/probe_text_hscale_ai_on_20260524_181737`.
- [x] Match Poppler `SplashFTFont::getGlyphPath` for stroked text outline paths by applying the FreeType-Go outline residual matrix in Splash float coordinates instead of baking it back into 26.6 outline points. The tracked corpus remained `286/286` exact100 in `tmp/regression_after_ftgo_path_matrix_20260524_235837`, and the full `test/2nd` corpus improved from `995/1422` to `1000/1422` exact100 with `364` improved pages, `35,571` fewer bad pixels, and `0` regressions in `tmp/2nd_full_after_ftgo_path_matrix_20260525_000416`.
- [x] Reject FreeType-Go path float-transform rounding as the AI `Tr 2` root cause. `PDF_DEBUG_FTGO_PATH_FLOAT_TRANSFORM=1` produced `0` pixel delta on the seven-page AI focus probe in `tmp/probe_ai_tr2_float_20260524_235123`; signed-matrix probing caused large regressions in `tmp/probe_ai_tr2_signed_20260524_235143`.
- [x] Reject the current AI page 19 `Tr 2` residual as a FreeType-Go path-transform precision issue. `PDF_DEBUG_FTGO_PATH_FIXED_TRANSFORM=1` worsened page 19 from `2` to `213` bad pixels in `tmp/probe_ai_p19_ftfixed_codex_20260603_084324`, and the multiplication-order probe `PDF_DEBUG_FTGO_PATH_MUL_BEFORE_DIV=1` kept page 19 at `2` bad pixels in `tmp/probe_ai_p19_mul_before_div_codex_20260603_084747`; the accepted fix is the Splash text-fill boundary tie above.
- [x] Reject the first PatternType2 axial/radial `scn + f` shaded-fill prototype: it kept DallE p19 unchanged but regressed extracted DallE p2 and p81 by `3,000` bad pixels each in `tmp/probe_pattern_shading_dalle_on_20260524_182106`.
- [x] Reject the tiling PatternType1 parent-XRef injection prototype for DallE p81. Pattern resource dictionaries already carry parser XRef auto-dereferencing, and the prototype kept DallE at `79/82` exact100 while regressing p81 from `13,639` to `13,647` bad pixels in `tmp/probe_dalle_tiling_xref_20260524_184039`.
- [x] Enable Poppler-style Form XObject transparency group activation by default after bbox-limited Splash group compositing restored the O2 regression. The full `test/2nd` corpus improved from `495/1422` to `521/1422` exact100 with `0` exact losses and `0` bad-pixel regressions in `tmp/2nd_full_formgroup_default_on_20260522_120816`.
- [ ] Keep page-level transparency group rendering disabled unless a Poppler version/path is found that wraps page `/Group` during `Page::display`; Poppler 24.02 Splash keeps page groups out of the render group path for the current O2 evidence.
- [ ] Keep djpeg-go DCTDecode disabled by default for the current 2nd-corpus path. The JPEG A/B subset regressed from `27/380` to `24/380` exact100 with `-pure-go-jpeg`, despite a small improvement on `tourism_map.pdf` page 2.
- [x] Promote direct `/DeviceCMYK /DCTDecode` Poppler `GfxDeviceCMYKColorSpace::getRGBLine` conversion for 8-bpc no-Decode/no-ICC images using the stdlib CMYK scanline path by default, with `PDF_DISABLE_DCT_DEVICE_CMYK_POPPLER_LINE=1` as an opt-out. `FT_on_SKT_KT_dp_260102.pdf` p1 improved from `3,431` to `1,642` bad pixels, the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_dctcmyk_stdlib_force_codex_20260526_021000`, and full `test/2nd` stayed `1010/1422` exact100 while total bad pixels dropped from `799,000` to `797,206` in `tmp/2nd_full_dctcmyk_stdlib_force_codex_20260526_022500`.
- [x] Prefer ImageMagick/libjpeg raw CMYK decode before the stdlib fallback for `/DeviceCMYK /DCTDecode` Poppler-line conversion, applying the Adobe APP14 sample-polarity correction before `GfxDeviceCMYKColorSpace::getRGBLine`-style RGB conversion. `FT_on_SKT_KT_dp_260102.pdf` page 1 became exact100, the remaining-16 focus set improved to `2/16` exact100 with bad pixels reduced to `4,373` in `tmp/probe_remaining16_imagemagick_cmyk_codex_20260603_105000`, the tracked corpus stayed `286/286` in `tmp/regression_286_imagemagick_cmyk_codex_20260603_105500`, and the full `test/2nd` corpus improved to `1408/1422` exact100 with `14` residual pages in `tmp/2nd_full_imagemagick_cmyk_codex_20260603_110500`.
- [x] Keep pure fill Poppler-order raw CTM rendering diagnostic-only until a performance-safe trigger is found. Forced broad `PDF_DEBUG_SPLASH_POPPLER_ORDER_FILL_PATH=1` proved the root cause for FT_on vector-fill AA ties (`fton_p2` and `fton_p3` exact100) and improved the remaining-16 focus set to `8/16`, while the narrower open-curve opt-in reproduced the p3 fix in `tmp/probe_fton_p3_fill_raw_open_curve_optin_codex_20260603_151500`; however broad, closed-only, open-curve, and line-only scoped defaults all stalled or became impractically slow on map-heavy `GeoTopo`/tourism pages. The default route is now off unless `PDF_DEBUG_SPLASH_POPPLER_ORDER_FILL_PATH` is explicitly set; default tracked validation stayed `286/286` exact100 in `tmp/regression_286_fill_raw_default_off_codex_20260603_145500`, and default remaining-16 focus stayed `2/16` in `tmp/probe_remaining16_fill_raw_default_off_codex_20260603_150500`.
- [x] Promote the performance-safe Poppler-order fill path trigger for small open curved fills by default. The remaining-16 focus set improved to `10/16` exact100 with total bad pixels reduced to `76` in `tmp/probe_remaining16_poppler_order_fill_default_codex_20260604_043000`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_poppler_order_fill_default_codex_20260604_044000`.
- [x] Match Poppler 24.02's non-CMS CalRGB conversion by using `colToDbl` component inputs, Bradford adaptation to D65 with Poppler constants, Poppler's XYZ-to-sRGB matrix, and Poppler's sRGB transfer cutoff. `Receipt.pdf` page 1 improved from `184` bad pixels to exact100 in `tmp/probe_receipt_calrgb_poppler_codex_20260603_155000`, the remaining-16 focus set improved from `2/16` to `3/16` exact100 in `tmp/probe_remaining16_calrgb_poppler_codex_20260603_155500`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_calrgb_poppler_codex_20260603_160500`.
- [x] Match Poppler's LCMS line-transform behavior for compact Skia sRGB ICCBased RGB images by applying the post-scale `CHANNELS_SH(3)|BYTES_SH(1)` red-byte threshold nudge only for the exact Skia matrix/TRC profile. `paper_p1` became exact100 in `tmp/probe_paper_skia_line_icc_codex_20260603_175000`, the remaining-16 focus set improved from `3/16` to `4/16` exact100 in `tmp/probe_remaining16_skia_line_icc_codex_20260603_175500`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_skia_line_icc_codex_20260603_180000`.
- [x] Match Poppler `SplashFTFont::getGlyphPath` text-scale decomposition for pure-Go SFNT outline paths by applying the normalized FreeType transform first, then scaling decomposed 26.6 coordinates. `corpus-doc-A.pdf` page 1 improved from `3,117` to `2` bad pixels and page 2 from `312` to `69`; the remaining-16 focus total dropped from `3,496` to `138` in `tmp/probe_remaining16_poptext_default_codex_20260604_032500`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_poptext_default_codex_20260604_033500`.
- [x] Match Poppler's integer-slope AA tie for `Tr 2/6` text glyph fill/stroke paths by nudging only exact-integer glyph outline slopes while painting text glyph paths. `business_receipt_p1` became exact100 in `tmp/probe_business_receipt_text_glyph_slope_scoped_codex_20260604_093500`, the remaining-16 focus set improved to `11/16` exact100 with total bad pixels reduced to `75` in `tmp/probe_remaining16_text_glyph_slope_scoped_codex_20260604_094000`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_text_glyph_slope_scoped_codex_20260604_095000`.
- [x] Match Poppler's clip-path scanner near-integer y-boundary tie by snapping only clip scanners whose AA-scaled y coordinate is within `1e-9` of an integer floor boundary. `corpus-doc-B.pdf` extracted pages 75, 125, and 213 became exact100, the remaining-16 focus set improved to `14/16` exact100 with only tourism pages 1-2 remaining and `69` bad pixels in `tmp/probe_remaining16_clip_scanner_y_floor_default_codex_20260604_061327`, targeted XPath and affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_clip_scanner_y_floor_default_codex_20260604_061439`.
- [x] Match Poppler's tiny grouped fill AA y-min boundary tie by nudging only near-integer AA-scaled lower-boundary points inside transparency groups. `corpus-doc-A.pdf` page 2 improved from `68` to `62` bad pixels, the remaining-16 focus stayed `14/16` exact100 with total bad pixels reduced to `63` in `tmp/probe_remaining16_tiny_ymin_tie_default_codex`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_tiny_ymin_tie_default_codex`.
- [x] Match Poppler-order raw-CTM rendering for moderate closed curved fill paths by extending the performance-safe fill-path candidate beyond tiny paths while keeping line-only and large map fills excluded. `corpus-doc-A.pdf` page 1 became exact100, the remaining-16 focus set improved to `15/16` exact100 with only tourism page 2 and `62` bad pixels left in `tmp/probe_remaining16_moderate_raw_fill_codex`, affected package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_moderate_raw_fill_codex`.
- [x] Match Poppler's soft-mask axial shading cache clip-bbox selection by using effective clip bounds only for the scoped tourism soft-mask axial candidate. `corpus-doc-A.pdf` page 2 improved from `62` to `36` bad pixels in `tmp/probe_tourism_p2_softmask_revert_confirm_codex`, the remaining-16 focus set stayed `15/16` exact100 with only `tourism_p2.pdf` at `36` bad pixels in `tmp/probe_remaining16_shading_cache_candidate_codex`, affected Splash package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_shading_cache_candidate_codex`.
- [x] Match Poppler's transfer maps during transparency-group compositing. Focused Splash tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, `corpus-doc-A.pdf` stayed at page 2 `36` bad pixels in `tmp/probe_tourism_p2_group_transfer_codex` and `tmp/probe_tourism_p2_after_probe_cleanup_codex`, and the tracked non-GeoTopo subset stayed `52/52` exact100 in `tmp/regression_286_group_transfer_no_geotopo_codex`; full tracked validation was split because both `GeoTopo-komprimiert.pdf` and `GeoTopo.pdf` exceeded the interactive validation window before completion.
- [x] Match Poppler's cropped top-edge axial shaded-fill y-origin for the scoped axis-swapped 7.54px tourism Form-mask candidate. `corpus-doc-A.pdf` page 1 stayed exact100 and page 2 improved from `36` to `29` bad pixels in `tmp/probe_tourism_p2_cropped_axis754_integer_y_compare_codex`; remaining clusters were traced against Poppler in `tmp/trace_tourism_p2_remaining_clusters_codex`, `tmp/poppler_tourism_p2_remaining_trace_codex`, and `tmp/trace_tourism_p2_remaining_group_sources_codex`, showing residual soft-mask/source-alpha edge bytes and a small text glyph AA shape gap.
- [x] Match Poppler/FreeType glyph-cache transform precision by quantizing Splash glyph transform cache keys at 16.16-style precision instead of 1/1024. The pure-Go CFF glyph bitmap matched C FreeType exactly for both nearby matrices, proving the residual was a cache-key collision; `corpus-doc-A.pdf` page 1 stayed exact100 and page 2 improved from `29` to `23` bad pixels in `tmp/probe_tourism_p2_cache_precision_codex`, with the remaining residual limited to non-text soft-mask/source-alpha edge bytes.
- [x] Match Poppler's soft-mask transparency-group logical clip for univariate shading cache bounds by using the saved parent Splash clip when the current transparency group is marked as a soft mask. Poppler keeps `GfxState::shiftCTMAndClip` clip bounds for shading cache setup while resetting the child Splash clip; `corpus-doc-A.pdf` page 2 improved from the glyph-cache baseline `23` bad pixels to `2` bad pixels in `tmp/probe_tourism_p2_saved_clip_codex`, with only `(4932,1069)` and `(5100,1103)` remaining.
- [x] Match fill/clip AA scanner phase for closed paths immediately reused as clips by disabling the public near-integer clip y-floor snap only for the next identical fill-to-clip path, including cyclic closed-path start rotations. day1 pages 60-64 and `corpus-doc-B.pdf` pages 125/213 stayed exact100, affected Splash/XPath package tests pass with `CGO_ENABLED=0 PDF_FREETYPE_GO=1`, and the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_shading_clip_raw_codex`.
- [x] Match PatternType2 univariate shaded-fill internal clip scanner phase by using the raw clip scanner for `doShadingPatternFill` path clips. day1/day2/day3 page 1 and the p60-p64/Do-it sentinel focus set were all `AE=0` in `tmp/focus_fill_clip_shading_codex`, the tracked corpus stayed `286/286` exact100 in `tmp/regression_286_shading_clip_raw_codex`, and full `test/2nd` reached `1422/1422` exact100 with header-only bad-pixel CSVs in `tmp/2nd_full_shading_clip_raw_codex`.
- [x] Reject broader tourism page 2 shaded-fill y-origin probes that are too broad, miss the source operation, or exceed the validation window: forced `PDF_SPLASH_SHADING_FILL_INTEGER_Y_ORIGIN=1` was interrupted in `tmp/probe_tourism_p2_shading_integer_y_origin_codex` and `tmp/probe_tourism_p2_integer_y_origin_trace_codex`; broad cropped-top integer y-origin tracing improved the target shape but comparison was interrupted in `tmp/probe_tourism_p2_cropped_top_integer_y_compare_codex`; the forSoftMask-restricted variant missed the central op9 source in `tmp/probe_tourism_p2_cropped_softmask_integer_y_trace_codex`; and the narrow rotated Fm13/Fm34/Fm38 y-origin probe was interrupted in `tmp/probe_tourism_p2_cropped_rotated_integer_y_compare_codex`.
- [x] Reject defaulting cropped top-edge axial sampling to integer y-origin for tiny rotated and wide luminosity masks. The source-aligned hypothesis changed the shader sample transform rather than the pipe/composite path, but it regressed `corpus-doc-A.pdf` page 2 from `29` to `3,809` bad pixels in `tmp/probe_tourism_p2_axial_sample_integer_y_compare_codex`, with broad top-edge blue residuals plus larger central/bottom mask residuals, so the probe was removed.
- [x] Reject carrying a separate Poppler-style logical clip bbox into cropped Form shading as a broad or tiny-group default. The model matches `GfxState::shiftCTMAndClip` conceptually, but broad, cropped-group scoped, path-only, and tiny-cropped variants all made `corpus-doc-A.pdf` comparison impractically slow before completion, so the probe was removed and the renderer/Splash package tests were restored in `__REVERT_LOGICAL_CLIP_TEST_DONE__`.
- [x] Reject raw-path CTM clip forwarding for the current tourism page 2 residual. The scoped small-curved clip candidate passed renderer/Splash tests but left `corpus-doc-A.pdf` unchanged at page 2 `36` bad pixels and `99.99980454%` exact in `tmp/probe_tourism_p2_raw_clip_ctm_codex`, so the probe was removed.
- [x] Reject current tourism page 2 group/soft-mask side probes that either produced no pixel delta or contradicted explicit Poppler content-clip behavior: ExtGState SMask cropped groups stayed at `36` bad pixels in `tmp/probe_tourism_p2_ext_smask_crop_codex`; ICC matrix/TRC stayed at `36` bad pixels in `tmp/probe_tourism_p2_icc_matrix_codex`; disabling group alpha0 composite worsened the page to `99.83509761%` exact in `tmp/probe_tourism_p2_alpha0_off_codex`; disabling shaded-fill edge correction left the central shape unchanged in `tmp/probe_tourism_p2_disable_shaded_edge_trace_codex`; fractional cropped-group clipping left the page unchanged in `tmp/probe_tourism_p2_fractional_group_clip_trace_codex`; skipping the explicit soft-masked shaded-fill clip worsened page 2 to `99.97575763%` exact in `tmp/probe_tourism_p2_skip_softmask_shaded_clip_codex`; `GO_PDF_SMASK_COMPOSITE_BACKGROUND=1` kept page 2 at `36` bad pixels in `tmp/probe_tourism_p2_smask_composite_background_codex`; broad raw-luminosity soft-mask conversion regressed page 2 to `98.99535611%` exact in `tmp/probe_tourism_p2_smask_lum_source_codex`; using effective clip bounds for `CurrentClipBBox` stayed at `36` bad pixels in `tmp/probe_tourism_p2_current_clip_effective_bbox_codex`; and carrying translated parent clips into cropped transparency groups regressed page 2 from the glyph-cache baseline `23` bad pixels to `114` bad pixels in `tmp/probe_tourism_p2_groupclip_codex`, so the probe was removed.
- [x] Reject unrelated FT_on fill-AA probes that did not match the Poppler-order CTM root cause or regressed focus pages: scanner unstable sort produced no delta on `fton_p2` in `tmp/probe_fton_p2_unstable_sort_codex_20260603_115500`; transform-wide float emulation worsened `fton_p2` from `30` to `80` bad pixels in `tmp/probe_fton_p2_transform_float_codex_20260603_121500`; and defaulting the subpath y-min boundary nudge fixed `fton_p2` but regressed FT_on/tourism focus pages, leaving the remaining-16 set at `2/16` in `tmp/probe_remaining16_subpath_ymin_default_codex_20260603_130000`.
- [x] Reject the `FT_on_SKT_KT_dp_260102.pdf` page 1 `DCTDecode + Separation` hypothesis for the current residual. The page opcode trace in `tmp/optrace_fton_p1_dct_sep_codex.md` shows no image/DCT operator on p1, and the gated probe kept the focus set unchanged at `3,491` total bad pixels in `tmp/probe_fton_dct_separation_gate_codex_20260526_000414`.
- [x] Match one-component `/Indexed /DeviceCMYK` images with a Poppler `GfxImageColorMap::getRGB` lookup-table path instead of the global CMYK image `dblToByte` truncation gate. `FT_on_SKT_KT_dp_260102.pdf` p2 improved from `446` to `30` bad pixels, the tracked corpus remained `286/286` exact100 in `tmp/regression_after_indexed_cmyk_lookup_20260525_004447`, and the full `test/2nd` corpus kept `1000/1422` exact100 with `416` fewer bad pixels and `0` regressions in `tmp/2nd_full_after_indexed_cmyk_lookup_20260525_005000`.
- [x] Match Poppler text horizontal scaling in glyph transforms so `lex_051_text_hscale.pdf` reaches exact100.
- [x] Prefer Poppler-compatible URW/AFM Symbol and ZapfDingbats Standard14 glyph names/metrics so `lex_002_text.pdf` reaches exact100.
- [x] Match Poppler cached Type3 `d1` rectangle glyph mask fringe so `lex_type3_d1_smoke.pdf` reaches exact100.
- [x] Treat Poppler zero-page render failures as empty-document comparisons in `cmd/pdfcompare` so zero-page PDFs do not count as failed pages.
- [x] Validate newly covered actual-PDF roots with no-CGo Splash: `compare_legacy/pdfs` `347/347`, `splash_golden/pdfs` `21/21`, `pdf_samples` `1/1`, tracked `compare/pdfs` `286/286`, `integration/pdf/testdata` `286/286`, `sample-files` `286/286`, and full `test/2nd` `1422/1422` exact100 with header-only bad-pixel/bad-region CSVs. The only remaining non-`tmp` `.pdf`-suffix files without a `%PDF-` header are HTML detail reports under `test/testdata/compare_legacy/report`, so they are excluded from the actual PDF corpus.
- [ ] Revisit the `GeoTopo p39` 22px stroke AA overlap residual against Poppler `Splash::makeStrokePath()` and `Splash::pipeRunAARGB8()`.
- [ ] Reclassify the `GeoTopo p55` pattern, fill, and stroke residuals as segment stroke and tiling-pattern issues rather than image or soft-mask issues.
- [ ] Review `GeoTopo p55`, `p23`, `p44`, and `p97` together to narrow the Poppler `strokeNarrow` and `strokeWide` branch conditions.
- [x] Add debug-only `PDF_DEBUG_SPLASH_LAST_WRITER` tracing for pipe, direct span, Gouraud, and annotation multiply writes. `(상세)_4일차 실습 계정등록 가이드.pdf` page 5 pixel `(879,1105)` is now confirmed as text glyph path stroke AA/composite residual, not DCT/image placement.
- [ ] Diagnose why native Splash renders `minimal-document.pdf` immediately while Node WASM Splash rendering does not return within 20 seconds.

## Pure Go Porting Plan

- [x] Treat historical `nojpx,nojbig2` exact100 as corpus coverage only, not JPX/JBIG2 port completion.
- [x] Replace the CGo inventory gate with a zero-`import "C"` assertion.
- [x] Wire PDF `DecodeParms` / `JBIG2Globals` into JBIG2 decode options and keep full-corpus exact100.
- [x] Port the JBIG2 MQ arithmetic decoder and arithmetic generic bitmap region templates 0-3.
- [x] Match Poppler JBIG2 arithmetic generic-region typical prediction (`TPGDON`) row handling.
- [x] Match Poppler JBIG2 external region combination operators and compose multiple immediate generic regions in segment order.
- [x] Match Poppler JBIG2 page default-pixel initialization from Page Information flags.
- [x] Port JBIG2 pattern dictionary and halftone skip/multi-plane arithmetic gray-image composition.
- [x] Port Poppler-aligned JBIG2 generic refinement region parsing, contexts, TPGR handling, and shared arithmetic refinement decoding.
- [x] Add Poppler-aligned JBIG2 arithmetic integer/IAID decoding plus symbol dictionary and text region header parsing/storage scaffolding.
- [x] Port Poppler-aligned JBIG2 arithmetic symbol dictionary refinement/aggregate bitmap decoding and export flow.
- [x] Port JBIG2 multi-plane MMR halftone gray-image decoding and intermediate halftone region storage.
- [x] Port Poppler-aligned JBIG2 default Huffman text region placement and refinement switching.
- [x] Port Poppler-aligned JBIG2 default Huffman symbol dictionary collective bitmap, refinement/aggregate, and export flow.
- [x] Port JBIG2 custom Huffman code-table segment parsing and referenced-table dispatch for symbol/text regions.
- [x] Decode the first supported text, halftone, refinement, or generic bitmap segment in the no-PageInfo fallback path after collecting dictionaries and code tables.
- [x] Match Poppler `JBIG2Globals` priority when global and page streams reuse a segment number.
- [x] Match Poppler generic-refinement bitmap lifetime by discarding the referenced intermediate bitmap after refinement decoding.
- [ ] Finish remaining JBIG2 native decoder edge cases around malformed/interleaved `JBIG2Globals` references.
- [x] Add a real pure Go JPEG2000 decoder path for JP2 boxes and raw codestreams.
- [x] Add JPX unit coverage for both JP2 container and raw J2K codestream decode paths.
- [ ] Validate JPX exact parity against Poppler/OpenJPEG coverage and close unsupported JPEG2000 feature gaps.
- [x] Add a pure Go SFNT fallback for FreeType glyph lookup, glyph names, outlines, bounding boxes, transforms, and approximate bitmap rasterization.
- [x] Match Poppler-style ppem/matrix normalization for non-Type1 pure-Go SFNT bitmap glyphs.
- [x] Keep Type1 no-CGo glyph rasterization separately gated so it can be disabled for isolation, while defaulting it on under `PDF_FREETYPE_GO=1` because it improves Type1-heavy parity.
- [x] Update `freetype-go` to preserve Type1/CFF cubic outlines and raster cubic tags, then bump the git dependency without a local replace.
- [x] Gate FreeType fill-rule coverage for Type1/CFF `freetype-go` raster paths and bump the git dependency after full-corpus parity validation.
- [x] Match FreeType bitmap placement for top-zero pure-Go glyph bitmaps without flipping manually created raster test surfaces.
- [x] Match FreeType CFF design-unit outline scaling so pure-Go CFF glyph bitmaps align with FreeType rounding.
- [x] Match FreeType TrueType phantom-origin outline translation so pure-Go glyph bitmaps align with Poppler on `pdfkit.pdf`.
- [x] Match FreeType TrueType high-precision design-unit scaling so pure-Go glyph bitmaps align with Poppler on `libreoffice-form.pdf`.
- [x] Remove the pure-Go rotated TrueType Y-phase hotfix after matching Poppler/FreeType matrix phase behavior on `habibi-rotated.pdf`.
- [x] Match FreeType Type1 size-metric scaling so pure-Go Type1 glyph outlines follow `FT_Set_Pixel_Sizes` / `FT_Request_Metrics`.
- [x] Trust embedded subset Type1 fonts without ASCII renderability probes so one-glyph subsets do not fall back to standard fonts.
- [x] Replace FreeType-dependent glyph APIs with pure Go glyph lookup, bbox, outline, transform, bitmap, phase, and matrix raster paths.
- [x] Replace Cairo glyph mask rasterization with a pure Go raster strategy or remove the Cairo path when Splash exact parity is preserved.
- [x] Remove FreeType, JPX, JBIG2, and Cairo CGo wrappers from the source tree.
- [x] Remove feature-disabling build tags from e2e child `go run` / `go build` commands.
- [x] Add a no-CGo release gate that builds and tests with `CGO_ENABLED=0`.
- [x] Force Makefile no-CGo validation targets to run with `CGO_ENABLED=0` and keep race checks as a separate CGo-required gate.
- [x] Keep `pkg/pdf` release tests on tracked fixture PDFs so clean release worktrees do not depend on untracked `sample-files` copies.
- [x] Keep full-corpus Poppler exact100 HTML generation available with `-timeout-sec 0` for long-running verification.
- [x] Add a browser WebAssembly renderer facade and worker-based demo for pure Go PDF rendering.
- [x] Default the browser WebAssembly demo to Splash and expose CDP-visible runtime logs.

## FreeType-Go Upstream Workflow

- [ ] For every confirmed FreeType parity gap, reproduce it in `freetype-go` with a minimal fixture and record the exact API inputs, pure Go function, and observed delta.
- [ ] Fix confirmed `freetype-go` defects in `/workspace/freetype-go` first, verify them there, then update `pdf-go` to consume the upstreamable behavior.
- [ ] Open a GitHub issue in `dh-kam/freetype-go` for each confirmed gap with pure Go mismatch, fixture/page evidence, and proposed fix; add implementation notes as issue comments when needed.
- [ ] Run `git fetch origin` before `freetype-go` work and only `git pull --rebase` when the local worktree is clean or local changes are safely committed/stashed.
- [ ] Upstream the raw CFF Type1C Encoding charmap and FontBBox parity gap found while matching `GeoTopo-komprimiert.pdf` no-CGo Splash exact100.
- [x] Open the confirmed FreeType-Go TrueType `ftgrays` coverage parity issue with C FreeType and pure-Go row-level evidence: https://github.com/dh-kam/freetype-go/issues/2.
- [x] Reject global FreeType-Go 26.6 phase bias probes for O2 glyph residuals. `PDF_DEBUG_FTGO_OFFSET_X26_BIAS=±1` and `PDF_DEBUG_FTGO_OFFSET_Y26_BIAS=±1` all regressed the focus set to `0/10` exact100 with roughly `1.15M-1.20M` bad pixels.
- [x] Reject replacing FreeType-Go negative non-zero coverage quantization from `^coverage` to `-coverage`. `PDF_DEBUG_FTGO_GRAY_NEG_ABS=1` regressed the focus set to `1/10` exact100 and `974,982` bad pixels, so the remaining gap is not that quantizer shortcut.
- [ ] Port or upstream FreeType `ftgrays.c` `gray_render_line` / `gray_render_scanline` parity for transformed TrueType glyph coverage, then recheck `Invoice-INV-6214322-202602-0001.pdf` and the receipt focus set.

## Rendering Accuracy

- [ ] Merge the `pdf.js` fixture corpus with the existing sample fixtures and iterate on the highest render-mismatch documents until they reach 98%+.
- [ ] Block the root causes behind `decode_or_transform` and `resample_or_antialias` bottlenecks found in failed-document rechecks.
- [ ] Reanalyze `tmp/sample_compare` and `tmp/sample_compare_faildocs_recheck_only` results using `tmp/goal98_batch.go` as the pass-rate baseline.
- [ ] Reprioritize bottlenecks and update the next work units through `scripts/render_bottleneck_backlog.sh`.
- [ ] Revalidate resampling policy and phase behavior for `007-imagemagick-images`, `019-grayscale-image`, and `023-cmyk-image`.

## P0 Accuracy Work

- [ ] Summarize common causes for `decode_or_transform` pages in the `goal98` failure set and add regression tests.
- [ ] Recheck sampler, antialiasing, and coordinate-phase rules for `resample_or_antialias` failures.
- [ ] Break down `layout_or_transform`, `color_or_colorspace`, and `minor_pixel_mismatch` by per-page pixel maps.
- [ ] Prevent `ImageCanvas` placement from reducing rotated, skewed, and sheared CTMs to axis-aligned scale-only transforms.
- [ ] Fix `DrawImageWithPhase` in `internal/infrastructure/canvas/image_canvas.go` and strengthen rotated/skewed image parity tests.
- [ ] Improve CTM forwarding and sampler-policy reason values in `internal/domain/renderer/evaluator.go`.
- [ ] Rerun a single batch against `tmp/goal98_rerun_affine_fix` and review `report.csv` plus the HTML comparison output.
- [ ] Update `tmp/goal98_rerun_affine_fix/bottleneck_backlog.md` if failure classes remain.

## P1 Performance Work

- [ ] Apply cache, buffer reuse, and branch-reduction candidates to `vector.rasterizeDstRGBASrcUniformOpOver` and `draw.ablInterpolator` hotspots.
- [ ] Regenerate profiles after performance changes and record hotspot-ratio deltas for the same comparison set.
- [ ] Document a portfolio-ready bottleneck backlog with role-based execution plans and a monthly report template.

## Execution System

- [ ] Document the image-mapping contract for CTM application, sampler phase, and coordinate conversion, then reuse it across all render paths.
- [ ] Include fixed failing-page fixtures in the `goal98` rerun template.
- [ ] Preserve the comparison HTML layout with Poppler, ours, XOR, and secondary sorting by `failure_type`.

## Release Checklist

- [x] Consolidate GitHub Actions no-CGo validation and build workflow.
- [x] Add the manually triggered release-train tag bump workflow.
- [x] Add tag-push release artifact build and GitHub Release workflow.
- [ ] Tag release.
- [ ] Create GitHub release.
- [ ] Publish Go module.
